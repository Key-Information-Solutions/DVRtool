bin/
obj/
*.user
.vs/
.env
*.lnk
downloads/
TestResults/
# build output, and exported fob rosters (live door credentials — never commit)
artifacts/
# iVMS name-enrichment: the imported name map and captured per-install DB keys are
# operator/customer data and secrets — never commit them.
identity-map*.json
*.ivmskey
ivms-keys/
# iVMS reconnaissance notes — hold a live SQLCipher key and real cardholder names.
# Kept as a local on-disk reference only; must never enter git history.
docs/ivms-integration-findings.md
